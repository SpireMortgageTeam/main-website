import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "Spire Mortgage Team — Calgary Mortgage Brokers",
  description: "Mortgage Solutions InSPIREd by You. Calgary-based mortgage brokers licensed in AB, BC, SK, and ON. 120+ lenders. Get the best rate for your situation.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="h-full antialiased">
      <body className="min-h-full flex flex-col">
        <Header />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
